#include "branching.hpp"

void callbacks::branching_callback(CPXCALLBACKCONTEXTptr context, const hplus::execution& exec, hplus::instance& inst, hplus::statistics& stats) {
    LOG_TODO << "branching callback";
}
